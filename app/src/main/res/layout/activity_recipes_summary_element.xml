<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recipe_summary_element"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/recipe_background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/recipe_name_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAlignment="center"
        app:layout_constraintBottom_toTopOf="@id/recipe_background_image"
        app:layout_constraintLeft_toRightOf="@id/recipe_background_image"
        app:layout_constraintRight_toLeftOf="@id/recipe_background_image"
        app:layout_constraintTop_toBottomOf="@+id/recipe_background_image"
        tools:text="Test text" />

</androidx.constraintlayout.widget.ConstraintLayout>